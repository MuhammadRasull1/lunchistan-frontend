# mockMenu.ts — данные меню

> Последнее обновление: 09.08.2026

- **59 реальных блюд** с локальными фото из `public/images/dishes/<категория>/`, `SET_PRICE = 55000`.
- Категории (поле `category`, тип `SetCategory`): `hot` (32), `salad` (13), `side` (8), `fastfood` (3), `appetizer` (2), `soup` (1).
- Каждое блюдо — сет: для горячих/фастфуда/супов состав = блюдо + салат + лепёшка + напиток; для салатов/гарниров/закусок = блюдо + напиток.
- КБЖУ генерируется детерминированно по категории (`kbjuFor`) с вариацией по индексу.
- Все пути к картинкам — реальные файлы в `public/images/dishes/` (проверено, отсутствующих нет).
- Импорт и сжатие фото: `scripts/import-images.mjs` (sharp, 1200px, q80).
- Подробнее о категориях и ценах → [[B2B_RULES#2-меню—59-реальных-блюд]].
